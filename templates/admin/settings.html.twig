{% extends 'base.html.twig' %}

{% block title %}{{ "Administrator settings"|trans }}{{ parent() }}{% endblock %}

{% block body %}
    {% include 'components/navbar.html.twig.html' %}

    <div class="container-fluid mt-3">

        <div class="row">
            <div class="col-12">
                {% include 'components/messages.html.twig' %}
            </div>
        </div>

        {{ form_start(form) }}
        {{ form_row(form.registration_enabled) }}
        {% include 'components/form-submit.html.twig' with {'title':'Submit'|trans } only %}
        {{ form_end(form) }}

    </div>
{% endblock %}



